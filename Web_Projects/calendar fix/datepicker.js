$(document).ready(function(){
	$("#datepicker").datepicker({ 
		autoclose: true, 
        todayHighlight: true,
        orientation: "auto",
        todayBtn: "linked",
    	clearBtn: true
  	});
});
